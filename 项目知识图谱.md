# 01-yca-AiManager 项目知识图谱

更新时间：2026-07-02

## 项目定位

AiManager 是基于 `gongqi210/litellm` 的公司级大模型 API 管理项目。员工和内部系统调用 AiManager 暴露的 OpenAI-compatible 接口；AiManager 保留 LiteLLM 的管理能力，并把已配置的上游模型调用统一收敛到 ycapi。

## 核心边界

- 上游入口只使用 `YCAPI_BASE_URL` 和 `YCAPI_API_TOKEN`。
- 默认 ycapi base 为 `https://ycapi.ycaicloud.com/v1`。
- 不在本项目配置 OpenAI、Anthropic、Gemini、Azure、Bedrock、Vertex 等直连供应商 key。
- `STORE_MODEL_IN_DB=false` 是 v1 安全默认值，避免通过 UI 绕过 YAML 中的 ycapi-only 模型清单。
- LiteLLM 管理面继续保留：Admin UI、virtual keys、teams、budgets、rate limits、usage/spend logs。

## 关键文件

| 文件 | 作用 |
| --- | --- |
| `PROJECT_TYPE` | AgentBrain 项目类型标识，当前为 `software-cli` |
| `AIMANAGER.md` | AiManager 根入口说明 |
| `cli/main.py` | AiManager 项目级 CLI 状态入口，支持 `--help` 和 `--json` |
| `cli/aimanager_cli_manifest.json` | AiManager P0/P1 运维与验收命令矩阵，包含 `admin_boundary_smoke`、`finance_export`、`wecom_alert_route`、`key_inventory_export`、`production_readiness`、`key_inventory_readiness`、`admin_boundary_readiness`、`finance_readiness`、`wecom_alert_readiness`、`live_ycapi_preflight`、`live_ycapi_roundtrip`、`work_context_enforcement_smoke`、`work_context_enforcement_readiness`、`production_policy_readiness`、`employee_monitoring_validate`、`lightweight_trial_evidence_capture`、acceptance gate 等 operator 入口 |
| `docs/architecture/allowed_deps.yml` | AiManager overlay 依赖白名单、FastAPI proxy hook 依赖登记、禁用 provider SDK import 和文件行数门禁配置 |
| `logs/README.md` | 日志落盘边界说明，禁止提交真实 runtime logs/secrets |
| `scripts/project_policy_check.py` | 项目级 policy gate fallback shim，按通用模板保留 `python3 scripts/project_policy_check.py` 入口，主体逻辑委托给 AiManager overlay |
| `aimanager/README.md` | 部署、运行、校验和 smoke test 说明 |
| `aimanager/config.yaml` | ycapi-only LiteLLM 配置；`general_settings.always_include_stream_usage=true` 是流式计费 usage chunk 的配置级门禁 |
| `aimanager/docker-compose.yml` | business surface + localhost-only admin profile + RBAC/DB-ready enabled management surface + Postgres 本地部署 |
| `aimanager/.env.example` | 非密钥示例环境变量，包含本地 business URL、员工 virtual key smoke 占位、生产边界/WeCom webhook/财务对账、生产政策 attestation、AC-23 trial evidence capture 变量、AC-26 policy register 默认路径和真实 roster/ack 占位 |
| `aimanager/asgi.py` | AiManager ASGI 入口，包裹 LiteLLM proxy app，按 business/management surface 执行路由策略，可启用 Postgres DB-ready fail-closed 预检；所有允许 `/v1/*` 推理路由都会按 LiteLLM 凭证头优先级从 Authorization、API-Key、x-api-key、x-goog-api-key、Ocp-Apim-Subscription-Key、x-litellm-api-key 提取 virtual key，并按 LiteLLM hash 查询 active/deleted 处置状态，冻结/撤销 key 直接 401 fail-closed，避免独立进程本地 key cache 延迟继续放行；business surface 额外拒绝 `LITELLM_MASTER_KEY`/`YCAPI_API_TOKEN`；business surface 在 `AIMANAGER_WORK_CONTEXT_ENFORCEMENT_ENABLED=True` 时对 chat/image 生成请求前置校验顶层 `user` 与完整 work-context `metadata`，缺失、非法场景或主体不一致返回 400 `aimanager_work_context_invalid`，且不把 prompt 传给 LiteLLM 或写入错误响应；在管理面可启用 trusted-header RBAC 高危写预检，在管理面 `POST /key/generate` 前置执行 key governance，在管理面 `POST /key/block`/`POST /key/delete` 前置要求处置人和原因并在成功后写 key lifecycle audit，对 `stream=true` chat 请求强制 `stream_options.include_usage=true` 以封堵流式计费绕过，同时将 chat body 缓冲限制为 32 MiB 并对异常 body fail-closed，记录低基数运行态 metrics，统一封装下游 LiteLLM/ycapi 4xx/5xx 和 SSE error event 为带 `request_id` 的 OpenAI-compatible error，并脱敏流式 secret，并在 LiteLLM proxy 加载后注册 image 计价和 shared-key enforced params guard |
| `aimanager/audit.py` | passthrough 拦截、shared-key enforced params 缺失、预算阻断、key 冻结/撤销的审计事件标准化 |
| `aimanager/redaction.py` | 验收与证据 artifact 共享脱敏模块：统一 Bearer、`sk-*`、WeCom webhook、常见 DSN 密码、secret 命名 env 值和 `TOKEN/API_KEY/SECRET/WEBHOOK/PASSWORD=...` 文本脱敏，并对 `os.environ/...` 占位、短布尔/通用 env 值做误脱敏保护 |
| `aimanager/business_overview.py` | M2 经营总览聚合：从月度财务 CSV、预算行和 observability report 生成总经理可读 JSON/Markdown，缺输入返回 BLOCKED，多币种返回 FAIL |
| `aimanager/employee_monitoring.py` | AC-26 员工监控制度校验：验证 metadata-only 监控字段、非工作时间异常和 key 共享规则、保留期、审阅角色、员工申诉和全员确认覆盖；缺真实证据 BLOCKED，矛盾制度 FAIL |
| `aimanager/finance.py` | LiteLLM SpendLogs 形态 usage/spend 记录规范化、日/月聚合、ycapi 账单行归一化和 AiManager-vs-ycapi 月度差异对账 |
| `aimanager/governance.py` | key 创建元数据、预算/限流字段和 shared key `enforced_params` 规范化与前置校验 |
| `aimanager/enforced_params_guard.py` | LiteLLM OSS proxy pre-call guard：读取 key metadata `enforced_params`，在推理前强制检查 `user`、`metadata.scenario_l1`、`metadata.end_user_principal`，缺失返回 400 `aimanager_enforced_params_missing` 并写 `enforced_params_blocked` 审计 |
| `aimanager/litellm_entrypoint.py` | 保留 LiteLLM CLI 初始化路径，把最终 uvicorn app 替换为 `aimanager.asgi:app`，对关键运行时 env 做 fail-fast，在导入 LiteLLM 前强制本地 model cost map，并把 `--config` 暴露给运行时 image 计价注册 hook，同时幂等注册 AiManager enforced params guard |
| `aimanager/lightweight_entry.py` | M2 非 SDK 入口适配层：把扁平 WeCom/轻量页/部门管理员表单校验为 work-context，并生成受控 `/v1/chat/completions` 请求体；要求财务 metadata，拒绝 token-like 字段和值，透传已校验的 external-marketing `workflow` 和 `brand_safety` 到 metadata，提交时只使用员工 LiteLLM virtual key |
| `aimanager/lightweight_web.py` | M2 AC-23 本地轻量 Web 入口：作为 business gateway 的 stdlib ASGI 客户端提供可点击内部试用页，服务端注入身份/财务默认值和员工 LiteLLM virtual key，不修改 business allowlist，不在 HTML 中暴露 key、Authorization 或 ycapi token，并对模型回答做 HTML escape |
| `aimanager/monthly_close.py` | AC-25 本地月结包：从月度财务 CSV、对账 CSV 和调整台账生成 JSON/CSV/Markdown，覆盖补记、冲销、归属调整、差异处理、残留未归属 BLOCKED 和非法台账 FAIL |
| `aimanager/observability.py` | 从结构化审计日志和 request-status rows 聚合 request id、失败率、429/5xx、预算阻断、passthrough 拦截、enforced params 拦截和机器可读 alert |
| `aimanager/policy.py` | M1 business/management surface 路由 allowlist、management-only Admin UI 静态/启动读接口白名单、passthrough/config/model-write 封堵、management-only `/metrics` 和 policy error body |
| `aimanager/runtime_metrics.py` | AiManager 运行态低基数 Prometheus 文本 metrics，包含 audit event counters 和 HTTP response counters |
| `aimanager/validation_errors.py` | 共享 Pydantic `ValidationError` 摘要格式化 helper，供 AC-23 trial evidence 与 evidence intake schema 校验复用，避免错误摘要格式漂移 |
| `aimanager/work_context.py` | M2 工作场景包校验：校验最终使用主体、场景标签、内外部、渠道、项目/客户、敏感级别、布尔审批要求、外发市场非空字符串 workflow 引用和品牌安全清单 |
| `aimanager/scripts/smoke_blocked_routes.py` | 对运行中 proxy 执行 provider/native/config/model-write/uncommitted 路由封堵矩阵 smoke，并验证业务面拒绝 master key 访问 `/v1/models` 与 `/v1/chat/completions` |
| `aimanager/scripts/mock_ycapi.py` | 本地 mock ycapi，提供 OpenAI-compatible chat/image 接口用于无真实 token 的运行态 smoke，chat 支持非流式 usage 和 SSE 流式最终 usage chunk，image 支持 `response_format=b64_json`；同时提供 `/v1/models` 作为 mock 完整性和 direct-check 口径，不代表 AC-06 会把 business `/v1/models` 转发到上游 |
| `aimanager/scripts/smoke_spend_logs.py` | 创建一次性治理 key，经业务面发 chat/image 请求，并查询 `LiteLLM_SpendLogs` 验证非零 spend；admin 写请求携带 `x-aimanager-role=proxy_admin` |
| `aimanager/scripts/smoke_sdk_compat.py` | 员工 SDK 兼容 smoke：用 employee virtual key 校验 `/v1/models`、chat、image b64、vision data URL、工作 metadata、OpenAI-compatible 响应形态和脱敏失败；缺 key 返回 BLOCKED，误用 `YCAPI_API_TOKEN` 变量或同值密钥返回 FAIL |
| `aimanager/scripts/smoke_runtime_sdk_compat.py` | AC-06 运行态 SDK 兼容 smoke：通过管理面创建一次性治理 employee LiteLLM virtual key，经业务面复用 SDK smoke，最后带 lifecycle audit headers 删除 disposable key；缺 master key 返回 BLOCKED，清理 HTTP/transport 失败返回 FAIL |
| `aimanager/scripts/smoke_budget_block.py` | 创建小预算一次性治理 key，经业务面先消费再验证 LiteLLM key budget 429 阻断；admin 写请求携带 `x-aimanager-role=proxy_admin` |
| `aimanager/scripts/smoke_key_lifecycle.py` | 创建一次性治理 key，验证管理面冻结/撤销后业务面通过 Authorization、API-Key、x-api-key、x-goog-api-key、Ocp-Apim-Subscription-Key、x-litellm-api-key 均被拒绝，并从 admin 容器日志确认 `key_frozen`/`key_revoked` 审计事件；admin 写请求携带 `x-aimanager-role=proxy_admin` |
| `aimanager/scripts/smoke_postgres_down.py` | 在 Postgres down 场景验证 readiness 503、provider passthrough 仍 403、员工业务调用 sanitized 503 |
| `aimanager/scripts/smoke_admin_ui_key_creation.py` | 真实 Chrome 驱动 LiteLLM Admin UI 创建 key：先验证缺 governance metadata 的 UI 请求 400 fail-closed，再补齐 metadata/预算/限流/有效期/team/model/key alias 后成功创建并删除 disposable key |
| `aimanager/scripts/smoke_admin_boundary.py` | 生产管理面暴露边界 preflight：业务 URL 伪造 trusted role header 仍不能访问管理/UI/key/config/metrics 路由；公开管理 URL 必须不可达、边缘 401/403/404 阻断或只跳转到 allowlisted SSO，若伪造 role 到达 AiManager key governance 则 FAIL |
| `aimanager/scripts/smoke_live_ycapi.py` | live ycapi token preflight：缺 `YCAPI_API_TOKEN` 返回 BLOCKED；有 token 时默认只读调用 ycapi `/models` 并校验期望模型，`--run-inference-roundtrip` 会在模型校验后执行受控 chat/image roundtrip；证据只保留状态码、usage/image 计数和 request id，不打印 token/URL/prompt/响应正文 |
| `aimanager/scripts/smoke_work_context_enforcement.py` | business surface 工作上下文强制运行态 smoke：从环境读取员工 virtual key，先对 chat/image 发送缺 `metadata` 的请求，要求 400 `aimanager_work_context_invalid` 且响应不回显 smoke prompt；`--run-valid-context-roundtrip` 会在 fail-closed 检查全部通过后，再用合法 work-context metadata 跑 chat/image 正向回路，要求 200、request id、chat usage 和 image 结果数；证据只保留 check type、method/path/model、状态、request id 和 usage/image 计数；缺员工 key 返回 BLOCKED；该 smoke 不替代 key inventory 或身份有效性验证 |
| `aimanager/scripts/export_key_inventory.py` | AC-08 生产 LiteLLM virtual key inventory 安全导出器：用 `LITELLM_MASTER_KEY` 逐页调用 management surface `/key/list?page=<n>&size=100&include_team_keys=true&return_full_object=true`，只写 allow-listed metadata-only inventory，丢弃 raw key/token/hash/masked `key_name` 等字段；发现缺可信 total、总数不匹配、分页漂移、metadata-only 输出仍含 secret-like 值、缺管理 key 或缺输出路径时不写 evidence 并返回 FAIL/BLOCKED |
| `aimanager/scripts/validate_key_inventory.py` | AC-08 生产 virtual key inventory 校验器：读取 `AIMANAGER_KEY_INVENTORY_FILE` 或 `--inventory-file` metadata-only JSON，要求 24 小时内且不超过 5 分钟未来偏差的全量导出 provenance（`exported_at`、`export_source`、`export_scope=all_virtual_keys`、`exported_by`、`expected_total_key_count`）且总数匹配；要求 active LiteLLM key 绑定 user/team、显式模型、正预算、RPM/TPM 限流、`duration` 和治理 metadata；shared key marker 必须是 JSON boolean，shared key 必须带完整 `enforced_params`；`--require-acknowledged-employees` 会读取员工监控制度、员工名册和确认记录，校验 active key `user_id` 属于 active 员工且已确认最新制度，并校验 key metadata 部门与名册一致；缺文件、缺 provenance、stale/future export、缺员工确认 evidence 或 active key 数为 0 时 BLOCKED，未替换 `template_marker`/`TEMPLATE_DO_NOT_SUBMIT`/`replace-with-*` 模板标记、legacy/unbounded/total-mismatch/non-boolean-shared/unacknowledged-or-unknown-user/raw-secret inventory FAIL 且脱敏 |
| `aimanager/scripts/production_readiness_bundle.py` | M1 生产试点前统一证据包：聚合 AC-15 生产管理面边界逐探测项结果、AC-20 生产 business surface 工作场景 fail-closed + valid-context roundtrip smoke、AC-19 live ycapi token、AC-08 production key inventory、AC-16 WeCom alert routing、AC-12/13 财务导出与 ycapi 账单对账、AC-POLICY 生产政策 attestation，输出脱敏 JSON；AC-08 强制要求 key inventory 与员工监控制度/名册/确认记录交叉验证，证据中保留 employee acknowledgment 摘要；AC-20 要求真实 `AIMANAGER_BUSINESS_BASE_URL` 与员工 virtual key 先验证 chat/image 缺 metadata 返回 400 `aimanager_work_context_invalid`，再验证合法 metadata chat/image 返回 200、request id、chat usage 和 image 结果数；AC-12/13 要求对账 CSV 无 `needs_review` 行，错账差异返回 FAIL；AC-POLICY 要求 ycapi 限额为有限正数、finance/security/legal 三方规范化后独立审批人；缺生产输入时 exit code 2 / BLOCKED |
| `aimanager/scripts/business_trial_acceptance_bundle.py` | M2 业务试用准入证据包：复用 production readiness，再聚合 AC-23 真人 5 分钟非技术入口试用证据和 AC-26 员工监控制度 evidence；AC-23 必须同轮 AC-19 live ycapi PASS 且 captured_at 不早于完成时间、不晚于本轮 bundle generated_at 才可 PASS，拒绝 prompt/response/token/key 原文并脱敏输出 |
| `aimanager/scripts/generate_launch_gap_plan.py` | M2/M3 启动缺口计划生成器：只读消费 `production_readiness_bundle` 与 `business_trial_acceptance_bundle` JSON，去重重复 AC，按 owner 输出 `FAIL`/`BLOCKED` 缺口、所需 env/files、重跑命令和下一步动作；AC-08-KEY-INVENTORY 分派给 security/ops，命令收敛为 `make key-inventory-readiness`，required env 包含 `AIMANAGER_KEY_INVENTORY_FILE`、`LITELLM_MASTER_KEY`、员工监控制度、员工名册和确认记录；AC-15 分派给 architecture/security/ops，命令收敛为 `make admin-boundary-readiness`；AC-20 分派给 product/engineering/security/ops，命令收敛为 `make work-context-enforcement-readiness`，下一步动作要求 fail-closed + valid-context roundtrip 且只保留 request id/usage/image 计数等脱敏元数据；AC-16-WECOM 分派给 ops，命令收敛为 `make wecom-alert-readiness`；AC-12-13-FINANCE 分派给 finance，命令收敛为 `make finance-readiness`；AC-19/AC-POLICY/AC-23/AC-26 缺口命令分别收敛为 `make live-ycapi-roundtrip`、`make production-policy-readiness`、`make lightweight-trial-evidence-capture`、`make employee-monitoring-validate`，AC-23 required env 覆盖 entry result、request/spend、operator role、identity source、key alias、时间戳、观察人和四项人工确认；写脱敏 JSON/Markdown；无输入时 exit code 2 / BLOCKED |
| `aimanager/scripts/generate_evidence_handoff.py` | M2/M3 证据移交包生成器：只读消费 `launch-gap-plan.json`，按 owner 生成 `evidence-handoff.json/md` 和 owner Markdown 请求文件；不解除 `FAIL`/`BLOCKED`，畸形输入或顶层非 PASS 但无 gap 时也会保留/合成非 PASS 输入 gap，不调用 live 服务，并提醒不得粘贴密钥、原始 prompt/response、cookie、员工 key、ycapi token 或客户内容 |
| `aimanager/scripts/generate_evidence_template_pack.py` | M2/M3 外部证据模板包生成器：只读消费 `launch-gap-plan.json`，生成 security/ops key inventory、finance/ops/AC-23/AC-26/AC-POLICY 安全输入模板和 `evidence-env.template`；key inventory 模板带 full-export provenance、`expected_total_key_count`、`duration` 占位；AC-23 operator role / identity source 与 `.env.example` 默认一致，operator confirmation env 默认 `false`，不自动确认 live ycapi/品牌安全/无 secret echo/HTML escape；模板带 `TEMPLATE_DO_NOT_SUBMIT`，不写 ycapi token 或 raw virtual key，Webhook 留空，且不能伪造 readiness PASS |
| `aimanager/scripts/validate_evidence_intake.py` | M2/M3 外部证据 intake 校验器：只读扫描 owner 回填 evidence 目录或 gate env 指向的 evidence 文件；未配置任何 env-pointed evidence 文件、未替换模板、空 CSV/表头文件返回 BLOCKED，raw prompt/response/header、cookie、Authorization 和 secret-like 值返回 FAIL，但允许 `raw_prompt_access=prohibited` 这类表达禁止原文访问的 governance policy 字段；对 AC-23 trial evidence 和 AC-POLICY production policy attestation 做 schema/业务字段校验，安全但矛盾的回填证据返回 FAIL；与 acceptance gate 配合校验 `AIMANAGER_KEY_INVENTORY_FILE` 等 env-pointed evidence |
| `aimanager/scripts/acceptance_coverage_matrix.py` | 最终验收覆盖矩阵 gate：解析 `docs/aimanager/1_acceptance_criteria.md`，校验 AC-01..AC-26 与 AC-POLICY 均映射到本地脚本/测试或 M1/M2 bundle check，检查本地 artifact 存在，叠加 `production_readiness_bundle` / `business_trial_acceptance_bundle` 状态，检测 `AC-08-KEY-INVENTORY`、`AC-12-13-FINANCE`、`AC-16-WECOM`、`AC-20` 等 bundle check 漂移，并输出脱敏 JSON/Markdown |
| `aimanager/scripts/generate_final_acceptance_report.py` | 最终验收报告生成器：只读消费 `business_trial_acceptance_bundle`、`generate_launch_gap_plan`、`acceptance_coverage_matrix` 和可选 `evidence-intake.json`，输出唯一 go/no-go JSON/Markdown；只有业务试用 bundle PASS、启动缺口为空、覆盖矩阵 PASS 且包含非空 criteria 对象列表、且已提供 evidence intake 为 PASS 时才返回 PASS，缺输入/未解除外部证据 BLOCKED，坏 JSON、坏 criteria 或失败 intake 项 FAIL；同时输出来源级 blocker 与去重 blocker 计数，并脱敏 Bearer、`sk-*`、DSN 密码和 WeCom webhook |
| `aimanager/scripts/run_acceptance_gate.py` | 一键最终验收 gate：在 run-scoped 输出目录中只执行一次 `production_readiness_bundle`，把同一份 readiness 注入 `business_trial_acceptance_bundle`，再生成 launch gap、owner 证据移交包、证据模板包、env-pointed evidence intake、acceptance coverage、final report 和 gate manifest JSON/Markdown；会把 `AIMANAGER_KEY_INVENTORY_FILE` 等 env-pointed evidence 先过 intake；避免手工串联旧 `/tmp` 文件，统一返回 0/1/2 并脱敏 Bearer、`sk-*`、DSN 密码、WeCom webhook 和 secret-valued env |
| `aimanager/scripts/capture_lightweight_trial_evidence.py` | AC-23 轻量入口试用证据采集器：从成功 `submit_lightweight_entry` 结果中只抽取 allow-listed metadata/endpoint/model/status，并要求操作者填入真实 request id、非零 spend、时间和观察人；要求提交结果携带 preflight `entry.work_context.status=PASS` 与匹配的 normalized work-context 标识，阻断裁剪或状态翻转式工作场景 PASS 绕过并降低完整手写 JSON 风险；支持 project/customer 二选一，拒绝 prompt/response/header/token/key 原文、secret-like 输出值、secret-like assistant echo 和 raw-key-looking alias |
| `aimanager/scripts/project_policy_check.py` | 项目级 policy gate 主体逻辑：检查脚手架、CLI contract、依赖白名单、ycapi-only config、`.env.example` 与 `.env` 忽略规则 |
| `aimanager/scripts/run_lightweight_web.py` | M2 AC-23 轻量 Web 入口启动脚本：默认绑定 `127.0.0.1:4002`，用 uvicorn 运行 `aimanager.lightweight_web:create_app` |
| `aimanager/scripts/submit_lightweight_entry.py` | M2 非 SDK 入口 CLI：从 lightweight form JSON dry-run 生成治理请求，或使用员工 virtual key 向 business `/v1/chat/completions` 原始 HTTP 提交；拒绝 `YCAPI_API_TOKEN` 作为员工 key |
| `aimanager/scripts/validate_work_context.py` | M2 非 SDK 入口后端契约 CLI：从 work-context JSON 校验 preflight/closure 工作场景和市场内容流程，缺文件 BLOCKED，不合规 FAIL |
| `aimanager/scripts/export_finance.py` | 从 LiteLLM SpendLogs JSON/CSV 和 ycapi 月账单 JSON/CSV 生成 AC-12/13 M1 财务导出 CSV，并返回 billable/reconciliation/`needs_review` 计数供 production readiness 判定 |
| `aimanager/scripts/generate_business_overview.py` | 从 `aimanager_finance_monthly.csv`、预算 CSV/JSON 和 observability JSON 生成 AC-24 经营总览 JSON/Markdown，缺预算/财务/观测文件 BLOCKED |
| `aimanager/scripts/generate_monthly_close_package.py` | 从 `aimanager_finance_monthly.csv`、`aimanager_reconciliation.csv` 和财务调整台账生成 AC-25 月结包 JSON、调整后 CSV 和 Markdown；缺输入 BLOCKED |
| `aimanager/scripts/validate_employee_monitoring_policy.py` | 从员工监控制度 JSON、员工名册和确认导出生成 AC-26 JSON/Markdown 验证结果；缺真实 evidence BLOCKED |
| `aimanager/scripts/export_observability.py` | 从 AiManager 审计日志和 request-status JSON/CSV 生成 observability JSON 报告和 alert 记录 |
| `aimanager/scripts/route_observability_alerts.py` | 从 observability JSON alert 生成企业微信 markdown payload，支持 dry-run、severity 过滤、缺 webhook 时 BLOCKED、WeCom `errcode=0` 校验且不打印 webhook URL |
| `aimanager/scripts/validate_config.py` | 配置边界校验脚本 |
| `aimanager/tests/test_audit.py` | 风险/审计事件合同测试 |
| `aimanager/tests/test_business_overview.py` | AC-24 经营总览合同测试，覆盖月费用、预算消耗、异常事件、TOP 部门/项目/key、缺预算 BLOCKED 和混币种 FAIL |
| `aimanager/tests/test_asgi_runtime.py` | Lazy LiteLLM proxy 加载后执行 image 计价注册 hook 和 enforced params guard 注册的运行时合同测试 |
| `aimanager/tests/test_blocked_route_smoke.py` | blocked-route smoke 脚本合同测试 |
| `aimanager/tests/test_config.py` | ycapi-only 配置回归测试，覆盖 `always_include_stream_usage=true` 流式计费门禁 |
| `aimanager/tests/test_finance.py` | 财务聚合、SpendLogs 形态归一化、ycapi 账单导入与对账基础测试 |
| `aimanager/tests/test_finance_export_script.py` | 财务 CSV 导出脚本合同测试 |
| `aimanager/tests/test_governance.py` | key governance、Admin UI 数字字符串规范化和 shared key enforced params 测试 |
| `aimanager/tests/test_enforced_params_guard.py` | shared-key OSS 推理期 enforced params guard 合同测试，覆盖缺字段 400、审计事件、空值判定、非 shared key 兼容和错误体脱敏 |
| `aimanager/tests/test_key_disposition_guard.py` | key/token disposition guard 合同测试，覆盖 business surface master/ycapi token 前置 403、business/management 推理面冻结 key 前置 401、LiteLLM 全部凭证头、x-litellm-api-key 优先级、无凭证交给 LiteLLM 原生认证、默认 Prisma checker 对 active blocked/deleted key 的判定 |
| `aimanager/tests/test_admin_ui_key_creation_smoke.py` | Admin UI key 创建 smoke 脚本的治理 metadata 与 captured payload 合同测试 |
| `aimanager/tests/test_litellm_entrypoint.py` | AiManager CLI wrapper app override 测试 |
| `aimanager/tests/test_mock_ycapi.py` | mock ycapi models/chat/image 响应合同测试，覆盖 OpenAI-compatible model list、非流式 chat usage、流式 SSE usage chunk 和 image `b64_json` 响应 |
| `aimanager/tests/test_sdk_compat_smoke.py` | 员工 SDK 兼容 smoke 合同测试，覆盖缺 virtual key BLOCKED、工作 metadata、双 chat 模型、image b64、vision data URL、OpenAI 响应形态和失败脱敏 |
| `aimanager/tests/test_runtime_sdk_compat_smoke.py` | disposable-key 运行态 SDK smoke 合同测试，覆盖管理面建 key、业务面 SDK smoke 调用、SDK runner 异常后清理状态、失败清理、清理 HTTP/transport 失败 fail-closed 和缺 master key BLOCKED |
| `aimanager/tests/test_observability.py` | observability 聚合、日志解析、失败率和 alert 合同测试 |
| `aimanager/tests/test_observability_export_script.py` | observability JSON 导出脚本合同测试 |
| `aimanager/tests/test_observability_alert_route_script.py` | 企业微信告警路由合同测试，覆盖 dry-run、缺 webhook BLOCKED、WeCom 成功/失败、severity 过滤和 CLI payload 输出 |
| `aimanager/tests/test_live_ycapi_smoke.py` | live ycapi token preflight 合同测试，覆盖缺 token BLOCKED、期望模型校验、token/URL/body 脱敏和结果文件写入失败不覆盖 BLOCKED 退出码 |
| `aimanager/tests/test_work_context_enforcement_smoke.py` | business surface 工作上下文强制 smoke 合同测试，覆盖 chat/image 缺 metadata fail-closed、合法 work-context chat/image 正向回路、真实 work-context validator 接受 smoke metadata、错误码校验、员工 key header、prompt 不回显和错误状态检测 |
| `aimanager/tests/test_policy.py` | M1 route allowlist、ASGI policy middleware、streaming chat usage 强制注入、管理面 key 创建治理接入测试 |
| `aimanager/tests/test_streaming_error_contract.py` | 流式 SSE 错误契约测试，覆盖 HTTP 200 event-stream error 事件规范化、Bearer/sk 脱敏、跨 chunk secret 脱敏和 request_id 注入 |
| `aimanager/tests/test_policy_chat_body.py` | ASGI business chat body 边界测试，覆盖非流式 body 字节保持、chat body 上限和异常 body fail-closed |
| `aimanager/tests/test_budget_block_smoke.py` | runtime budget-block smoke 脚本合同测试 |
| `aimanager/tests/test_employee_monitoring.py` | AC-26 员工监控制度合同测试，覆盖 metadata-only、禁止 prompt/response/raw IP/customer content、off-hours、key sharing、审阅边界、提交的 policy register 与 validator 契约一致、确认覆盖、缺文件 BLOCKED 和秘密不外泄 |
| `aimanager/tests/test_key_lifecycle_smoke.py` | runtime key freeze/revoke smoke 脚本合同测试 |
| `aimanager/tests/test_lightweight_entry.py` | M2 非 SDK 入口适配合同测试，覆盖扁平表单转治理请求、财务 metadata、external-marketing workflow/brand_safety metadata 透传、大小写保留、token-like 输入拒绝、缺员工 key BLOCKED、dry-run CLI 和 YCAPI token 误用拒绝 |
| `aimanager/tests/test_lightweight_web.py` | M2 AC-23 本地轻量 Web 入口合同测试，覆盖内部场景限定、服务端默认值注入、server-side employee key、缺 key BLOCKED、YCAPI token 误用拒绝、secret prompt 不回显和模型回答 HTML escape |
| `aimanager/tests/test_monthly_close.py` | AC-25 月结包合同测试，覆盖补记、冲销、归属调整、差异处理、真实导出复合行键、未归属 BLOCKED、非法台账 FAIL、CLI 输出和 token-like 字段不外泄 |
| `aimanager/tests/test_project_policy_check.py` | 项目级 policy gate 合同测试，覆盖当前脚手架 PASS、缺脚手架 FAIL、`cli/main.py --json` 机器可读输出、`finance_export`/`wecom_alert_route`/AC-19/AC-POLICY/AC-23/AC-26 manifest 命令值和对应 Makefile operator target，并真实执行 `make lightweight-trial-evidence-capture` 验证 AC-23 缺人工确认时先在 Makefile guard 以 exit 2/BLOCKED 停止、四项确认全为 true 时才把确认 flags 传入脚本 |
| `aimanager/tests/test_spend_log_smoke.py` | runtime spend smoke 脚本合同测试 |
| `aimanager/tests/test_admin_boundary_smoke.py` | 生产管理面边界 preflight 合同测试，覆盖业务端伪造 role header、公开管理 URL 不可达/SSO/裸露失败和缺生产 URL BLOCKED |
| `aimanager/tests/test_key_inventory.py` | AC-08 production key inventory 合同测试，覆盖 governed active key PASS、员工名册/确认记录交叉验证 PASS、unknown/unacknowledged/wrong-department active key FAIL、要求员工确认 evidence 但缺文件 BLOCKED、blocked legacy key 跳过、空或全 blocked 清单 BLOCKED、缺 trusted provenance BLOCKED、stale/future export BLOCKED、declared total mismatch FAIL、active legacy/wildcard/unbounded/missing duration/missing metadata FAIL、shared key 缺 enforced params FAIL、非布尔 shared_key marker FAIL、未替换 evidence 模板标记 FAIL、raw secret-like 值脱敏 FAIL、缺 input BLOCKED 和 CLI JSON 输出 |
| `aimanager/tests/test_key_inventory_export.py` | AC-08 key inventory 安全导出合同测试，覆盖 `/key/list` 调用参数和分页、raw key/token/hash/masked `key_name` 字段不落盘、allow-listed metadata-only 输出可被 validator PASS、缺可信 total/分页片段/分页漂移/非终止分页/malformed response/transport error FAIL 不写文件、metadata secret-like FAIL 脱敏、可选 result JSON 写失败不覆盖 inventory PASS、缺 `LITELLM_MASTER_KEY` BLOCKED |
| `aimanager/tests/test_production_readiness_bundle.py` | 生产 readiness bundle 合同测试，覆盖缺生产输入 BLOCKED、FAIL 优先级、AC-15 逐探测项证据、AC-20 生产 work-context smoke PASS/FAIL/BLOCKED、合法 work-context roundtrip 元数据证据和员工 key 脱敏、AC-08 key inventory + employee acknowledgment 交叉证据 BLOCKED/PASS/FAIL、ycapi token/WeCom webhook/secret-like attestation 脱敏、WeCom 0 投递 BLOCKED、CLI JSON 输出、真实 finance export 文件生成、对账 `needs_review` FAIL、空财务输入和非计费占位财务输入 BLOCKED、AC-POLICY 示例结构、token 不可见、三方角色、非正/非有限限额、文件缺失/坏 JSON 和规范化独立审批人校验 |
| `aimanager/tests/test_business_trial_acceptance_bundle.py` | 业务试用准入 bundle 合同测试，覆盖 AC-23 同轮 AC-19 依赖、非技术试用证据 schema、captured_at 完成/出包时序边界、非法 generated_at 拒绝、token/raw-content 拒绝与脱敏、AC-26 状态传播、AC-POLICY 从 M1 production bundle 透传和 CLI exit code |
| `aimanager/tests/test_launch_gap_plan.py` | 启动缺口计划合同测试，覆盖 M1/M2 gate 去重、冲突状态 FAIL 优先级、AC-08-KEY-INVENTORY 分派给 security/ops 并收敛到 `make key-inventory-readiness` 且 required env 覆盖 key inventory、master key、员工监控制度、名册和确认记录、AC-15 收敛到 `make admin-boundary-readiness`、AC-16 企业微信收敛到 `make wecom-alert-readiness`、AC-12/13 财务收敛到 `make finance-readiness`、AC-19/AC-POLICY/AC-23/AC-26 缺口收敛到 env-driven `make` target、AC-23 完整 operator env、owner 分组、required env 继承、无输入 BLOCKED、JSON/Markdown 输出和 Bearer/`sk-*`/WeCom webhook 脱敏 |
| `aimanager/tests/test_evidence_handoff.py` | 证据移交包合同测试，覆盖 unresolved gaps 按 owner 分组、顶层非 PASS/畸形 plan 不被伪装为 PASS、owner Markdown/manifest 写入、秘密脱敏和禁止粘贴密钥/原文提示 |
| `aimanager/tests/test_evidence_template_pack.py` | 证据模板包合同测试，覆盖 key inventory/finance/ops/AC-23/AC-26/AC-POLICY 模板文件生成、AC-23 operator role / identity source 默认值、key inventory full-export provenance 字段、secret-like 文本脱敏、`YCAPI_API_TOKEN`/`LITELLM_MASTER_KEY` 只保留 secret-manager 注释且不写 export/value、Webhook 空值、CLI 非 PASS 退出码，以及模板无法让生产 readiness 伪装通过 |
| `aimanager/tests/test_evidence_intake.py` | 外部证据 intake 合同测试，覆盖未替换模板包 BLOCKED、空模板 CSV 阻断、secret-like/raw prompt/header evidence FAIL 且不回显密钥，允许 `raw_prompt_access=prohibited` 这类 metadata-only policy 边界字段，AC-23 trial evidence 和 AC-POLICY attestation 的 schema/业务字段错误 FAIL，以及安全填充文件 PASS |
| `aimanager/tests/test_acceptance_coverage_matrix.py` | 最终验收覆盖矩阵合同测试，覆盖真实 AC 文档完整映射、M1/M2 bundle overlay、AC-20 生产 bundle check 映射、合并 check 映射、缺失 bundle check FAIL、缺失本地 artifact FAIL、CLI JSON/Markdown 和 secret-like 脱敏 |
| `aimanager/tests/test_final_acceptance_report.py` | 最终验收报告合同测试，覆盖缺输入 BLOCKED、全绿才 PASS、失败输入和失败 evidence intake 均 FAIL、启动缺口保留 owner/command/required env、来源级/去重 blocker 计数、坏 JSON FAIL、空 criteria 和非对象 criteria 覆盖矩阵假 PASS 防护，以及 Bearer/`sk-*`/DSN/WeCom 脱敏 |
| `aimanager/tests/test_acceptance_gate.py` | 一键验收 gate 合同测试，覆盖 run-scoped artifacts、同目录 owner 证据移交包/模板包/env-pointed intake、`AIMANAGER_KEY_INVENTORY_FILE` evidence intake、production readiness 只执行一次并复用于 business trial、旧 artifact 覆盖免疫、注入全绿路径、提交的 golden fixture 驱动真实 collector 到 PASS、key owner/财务账单/政策审批/试用时间/员工确认五类 golden 证据篡改必须使 gate FAIL、空环境 BLOCKED CLI、unsafe env evidence FAIL、安全 env evidence PASS、env secret 值和常见 DSN 密码脱敏，以及所有输出 artifact 的 secret-like 脱敏 |
| `aimanager/tests/fixtures/acceptance_golden/` | 合成验收证据 fixture，metadata-only、无密钥，用于本地 acceptance gate golden E2E；只证明 gate 编排和真实 collector 的全绿路径，不代表生产 key inventory、live ycapi、WeCom、财务账单、政策审批、真人试用或 HR/法务确认已完成 |
| `aimanager/tests/test_redaction.py` | 共享脱敏模块合同测试，覆盖 Bearer、`sk-*`、WeCom webhook、Postgres/MySQL/MariaDB/Redis/Mongo DSN 密码、secret env 长值脱敏、短 env 边界和短布尔/通用 env 值不误脱敏，以及 `contains_secret_like` 结构化 secret 判定 |
| `aimanager/tests/test_lightweight_trial_evidence_capture.py` | AC-23 轻量试用证据采集合同测试，覆盖安全 evidence 输出、prompt/assistant/secret/header/key 不落盘、非 PASS 提交 BLOCKED、缺失或失败 work-context proof BLOCKED、metadata/normalized-context 非 preflight BLOCKED、normalized 标识不匹配 BLOCKED、metadata 与 normalized 大小写差异 PASS、secret echo/secret-like metadata/secret-like alias FAIL、project/customer 二选一、live ycapi 和确认 flags 必填 |
| `aimanager/tests/test_work_context.py` | M2 工作场景包合同测试，覆盖外发市场 preflight、closure 全流程引用、非法场景组合、外发未审批、缺品牌安全、非字符串 contract/workflow 引用、项目/客户二选一、CLI PASS/FAIL/BLOCKED 输出和 malformed JSON |
| `docs/aimanager/` | 调研、PRD、验收标准、多角色评审和验收报告 |
| `docs/aimanager/aimanager-employee-monitoring-policy.json` | AC-26 员工监控制度机器可读 policy register，编码 metadata-only 字段、禁止采集原始 prompt/response/raw IP/customer content、90 天保留、off-hours/key-sharing 异常规则和审阅权限边界；不包含真实 roster/ack evidence |
| `docs/aimanager/2_detailed_design_synthesis_v1.md` | AiManager M1 详细设计汇总基线 |
| `docs/aimanager/2_detailed_design_codex_v1.md` | Codex 版详细设计 |
| `docs/aimanager/2_detailed_design_claude_v1.md` | Claude 版详细设计 |
| `docs/aimanager/2_review_codex_on_claude_v1.md` | Codex 对 Claude 设计的评审 |
| `docs/aimanager/2_review_claude_on_codex_v1.md` | Claude 对 Codex 设计的评审 |
| `docs/aimanager/3_employee_monitoring_policy_v1.md` | AC-26 员工监控制度和生产解除条件说明 |
| `docs/aimanager/production_policy_attestation.example.json` | AC-POLICY 生产政策 attestation 示例结构：showback/chargeback、价格审批、ycapi token 限额、员工 virtual-key-only、数据边界、finance/security/legal 三方独立审批 |

## 已启用模型

| 对外模型名 | LiteLLM upstream | 状态 |
| --- | --- | --- |
| `gemini-2.5-flash` | `openai/gemini-2.5-flash` | 启用，chat/vision |
| `deepseek-chat` | `openai/deepseek-chat` | 启用，chat |
| `ycapi-image-1` | `openai/ycapi-image-1` | 启用，image |

## 暂缓模型

`ycapi-video-1` 暂不放进 LiteLLM `model_list`。原因是 ycapi video 是异步创建和轮询语义，和 LiteLLM 当前内置 OpenAI video 路由不完全一致；后续应新增 ycapi video adapter，或增加经过认证、计费和日志审计的 passthrough route。

## 需求文档状态

- 最新 PRD：`docs/aimanager/1_PRD_v3.md`
- 多角色评审：`docs/aimanager/1_multi_role_review_v2.md`
- 最新验收矩阵：`docs/aimanager/1_acceptance_criteria.md`
- 最新详细设计基线：`docs/aimanager/2_detailed_design_synthesis_v1.md`
- 详细设计互评产物：`docs/aimanager/2_detailed_design_codex_v1.md`、`docs/aimanager/2_detailed_design_claude_v1.md`、`docs/aimanager/2_review_codex_on_claude_v1.md`、`docs/aimanager/2_review_claude_on_codex_v1.md`
- v3 P0 关键修订：封堵 LiteLLM provider passthrough、非零计价和预算阻断、财务对账、RBAC、SDK/错误契约、市场内容流程、UI 信息架构和状态矩阵。
- 进入 M1 前必须确认：运行时 passthrough 直连路径已封堵；不能只依赖 `aimanager/config.yaml` 静态校验。

## 详细设计裁决

- 最终基线不是单独采用 Codex 版或 Claude 版，而是采用 Codex 的治理/财务/RBAC/分期结构，加上 Claude 的源码锚点、`aimanager/asgi.py` fail-closed 白名单、Google 原生路由封堵、`input_cost_per_image` 图片计价修正和 `enforced_params` 共享 key 约束。
- M1 业务 API 只承诺 `GET /v1/models`、`POST /v1/chat/completions`、`POST /v1/images/generations`。`/v1/completions`、`/v1/embeddings`、无 `/v1` 前缀兼容路径和 ycapi 专有 API 均不开放。
- M1 默认 business surface 只承诺业务 API 与 health checks；LiteLLM Admin UI、key/team/user/budget/spend 管理路由不在业务端口开放。
- M1 management surface 只用于受控管理入口：本地 compose 通过 `admin` profile 启动 `aimanager-admin`，绑定 `127.0.0.1:4001`，保留 LiteLLM UI/key/team/user/budget/spend 管理能力。
- 运行时主边界是进程内 allowlist 中间件，而不是 provider denylist。Edge Guard 只作为边缘加固。
- M1 必须在 business 与 management 两个 surface 都拒绝 `llm_passthrough_router`、`pass_through_router`、`google_router` 原生 Gemini 路由、`/pass-through-endpoints`、`/config/update`、高危 `/config/field/*`、`/config/cost_*`、`/config_overrides/*`、`/cache/settings`、`/reload/*` 和模型写路径。
- `STORE_MODEL_IN_DB=false` 只是第二道兜底；模型写入和 runtime config 更新必须在 AiManager 白名单层显式 403。
- `ycapi-image-1` 图片价格必须使用 LiteLLM 实际读取的 `input_cost_per_image > 0`，不能用 `output_cost_per_image` 作为有效计价字段。
- `ycapi-image-1` 的 provider-prefixed runtime key `openai/ycapi-image-1` 必须在 LiteLLM proxy 加载后重新注册 image 单价，否则 LiteLLM 可能成功返回图片但把 `LiteLLM_SpendLogs.spend` 记为 0。
- chat 模型必须显式配置 `input_cost_per_token` 和 `output_cost_per_token` 的 ycapi 转售价，避免同名内置模型价格静默继承。
- AC-05/AC-09 的关键证据是 chat/image 真实调用写入 `LiteLLM_SpendLogs.spend` 非零金额，不能只检查配置字段。
- shared key 主体透传使用 LiteLLM metadata `enforced_params`；AiManager OSS `AiManagerEnforcedParamsGuard` 在 LiteLLM proxy pre-call 阶段强制检查 `user`、`metadata.scenario_l1`、`metadata.end_user_principal`，缺失请求直接 400 并写 `enforced_params_blocked` 审计。
- M1 错误契约采用 body 注入方案：错误响应保留 OpenAI-compatible `error`，并在顶层提供 `request_id`，同时保留 `x-litellm-call-id` 响应头；下游 JSON error 保留 `message`/`type`/`param`/`code` 并做敏感串脱敏，下游非 JSON 4xx/5xx 规范化为通用 upstream error；HTTP 200 SSE error event 也会事件级规范化/脱敏，避免 Bearer、`sk-*`、ycapi token 文本或 DSN 密码泄漏。

## 验证命令

```bash
make policy-check
uv run --no-project --with pyyaml python -m aimanager.scripts.validate_config aimanager/config.yaml
PYTHONPATH="$PWD" uv run --no-project --with pytest --with pyyaml pytest aimanager/tests -q
make finance-export  # reads AIMANAGER_SPEND_FILE, AIMANAGER_YCAPI_BILL_FILE, optional AIMANAGER_FINANCE_OUTPUT_DIR
make key-inventory-readiness  # export + validate AC-08 inventory with employee acknowledgment cross-check, then refresh production readiness
make admin-boundary-readiness  # AC-15 production admin boundary preflight + production readiness
make finance-readiness  # AC-12/13 finance export + production readiness
make wecom-alert-readiness  # AC-16 dry-run alert payload + production readiness
make production-readiness  # unified M1 production evidence bundle
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.export_observability --audit-log-file /path/to/aimanager.log --request-status-file /path/to/request-status.csv --output-file /tmp/aimanager-observability.json
make wecom-alert-route  # reads AIMANAGER_OBSERVABILITY_REPORT_FILE, AIMANAGER_WECOM_WEBHOOK_URL, optional AIMANAGER_WECOM_MIN_SEVERITY
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.export_key_inventory --admin-base-url "$AIMANAGER_ADMIN_BASE_URL" --output-inventory-file "$AIMANAGER_KEY_INVENTORY_FILE" --output-json-file /tmp/aimanager-key-inventory-export.json
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.generate_business_overview --finance-monthly-file /tmp/aimanager-finance-export/aimanager_finance_monthly.csv --budget-file /path/to/aimanager-budgets.csv --observability-report-file /tmp/aimanager-observability.json --month 2026-06 --output-json-file /tmp/aimanager-business-overview.json --output-markdown-file /tmp/aimanager-business-overview.md
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.generate_monthly_close_package --finance-monthly-file /tmp/aimanager-finance-export/aimanager_finance_monthly.csv --reconciliation-file /tmp/aimanager-finance-export/aimanager_reconciliation.csv --adjustment-file /path/to/aimanager-monthly-adjustments.csv --month 2026-06 --output-json-file /tmp/aimanager-monthly-close.json --output-adjusted-csv-file /tmp/aimanager-monthly-close-adjusted.csv --output-markdown-file /tmp/aimanager-monthly-close.md
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.validate_employee_monitoring_policy --policy-file /path/to/aimanager-employee-monitoring-policy.json --employee-roster-file /path/to/employee-roster.csv --acknowledgment-file /path/to/employee-monitoring-acknowledgments.csv --output-json-file /tmp/aimanager-employee-monitoring.json --output-markdown-file /tmp/aimanager-employee-monitoring.md
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.smoke_live_ycapi --expect-model gemini-2.5-flash --expect-model deepseek-chat --expect-model ycapi-image-1 --run-inference-roundtrip
make work-context-enforcement-smoke  # reads AIMANAGER_BUSINESS_BASE_URL and AIMANAGER_EMPLOYEE_VIRTUAL_KEY
make work-context-enforcement-readiness  # AC-20 fail-closed + valid-context production readiness
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.production_readiness_bundle --output-json-file /tmp/aimanager-production-readiness.json
PYTHONPATH="$PWD" uv run --no-project --with pytest --with pyyaml pytest aimanager/tests/test_production_readiness_bundle.py::test_production_policy_attestation_example_file_is_valid -q
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.business_trial_acceptance_bundle --output-json-file /tmp/aimanager-business-trial-acceptance.json
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.generate_launch_gap_plan --production-readiness-file /tmp/aimanager-production-readiness.json --business-trial-file /tmp/aimanager-business-trial-acceptance.json --output-json-file /tmp/aimanager-launch-gap-plan.json --output-markdown-file /tmp/aimanager-launch-gap-plan.md
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.generate_evidence_handoff --launch-gap-plan-file /tmp/aimanager-acceptance-gate/launch-gap-plan.json --output-dir /tmp/aimanager-evidence-handoff
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.generate_evidence_template_pack --launch-gap-plan-file /tmp/aimanager-acceptance-gate/launch-gap-plan.json --output-dir /tmp/aimanager-evidence-template-pack
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.validate_evidence_intake --input-dir /tmp/aimanager-evidence-template-pack --output-json-file /tmp/aimanager-evidence-intake.json --output-markdown-file /tmp/aimanager-evidence-intake.md
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.acceptance_coverage_matrix --production-readiness-file /tmp/aimanager-production-readiness.json --business-trial-file /tmp/aimanager-business-trial-acceptance.json --output-json-file /tmp/aimanager-acceptance-coverage.json --output-markdown-file /tmp/aimanager-acceptance-coverage.md
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.generate_final_acceptance_report --business-trial-file /tmp/aimanager-business-trial-acceptance.json --launch-gap-plan-file /tmp/aimanager-launch-gap-plan.json --acceptance-coverage-file /tmp/aimanager-acceptance-coverage.json --evidence-intake-file /tmp/aimanager-evidence-intake.json --output-json-file /tmp/aimanager-final-acceptance-report.json --output-markdown-file /tmp/aimanager-final-acceptance-report.md
PYTHONPATH="$PWD" uv run --no-project --with pyyaml python -m aimanager.scripts.run_acceptance_gate --output-dir /tmp/aimanager-acceptance-gate
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.validate_work_context --context-file /path/to/work-context.json --mode preflight --output-json-file /tmp/aimanager-work-context.json
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.submit_lightweight_entry --form-file /path/to/lightweight-form.json --dry-run --output-json-file /tmp/aimanager-lightweight-entry.json
AIMANAGER_EMPLOYEE_VIRTUAL_KEY="$EMPLOYEE_VIRTUAL_KEY" PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.submit_lightweight_entry --form-file /path/to/lightweight-form.json --base-url http://localhost:4000 --output-json-file /tmp/aimanager-lightweight-entry-submit.json
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.capture_lightweight_trial_evidence --lightweight-entry-result-file /tmp/aimanager-lightweight-entry-submit.json --request-id <request-id-from-live-request-or-spend-log> --spend <nonzero-spend-from-litellm-spend-log> --operator-role marketing --identity-source sso --employee-virtual-key-alias <employee-key-alias-not-raw-key> --started-at <iso8601-started-at> --completed-at <iso8601-completed-at> --observer <observer-name> --captured-at <iso8601-captured-at> --live-ycapi-confirmed --brand-safety-confirmed --no-secret-echo-confirmed --html-escaped-confirmed --output-json-file /tmp/aimanager-ac23-trial-evidence.json
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.smoke_sdk_compat --base-url http://localhost:4000
LITELLM_MASTER_KEY=aimanager-local-master-key PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.smoke_runtime_sdk_compat --master-key aimanager-local-master-key --business-base-url http://localhost:4000 --admin-base-url http://localhost:4001
/Volumes/AI-projects/00-agent-brain/scripts/wsl_dev_docker_ssh.sh 'cd /home/yca-admin/projects/AI-projects/01-yca-AiManager && docker compose -f aimanager/docker-compose.yml config'
/Volumes/AI-projects/00-agent-brain/scripts/wsl_dev_docker_ssh.sh 'cd /home/yca-admin/projects/AI-projects/01-yca-AiManager && docker compose -f aimanager/docker-compose.yml --profile admin config'
LITELLM_MASTER_KEY=aimanager-local-master-key PYTHONPATH="$PWD" uv run --no-project --with pyyaml python -m aimanager.scripts.smoke_spend_logs --master-key aimanager-local-master-key --business-base-url http://localhost:4000 --admin-base-url http://localhost:4001 --project-directory "$PWD" --poll-attempts 60 --poll-interval 1
LITELLM_MASTER_KEY=aimanager-local-master-key PYTHONPATH="$PWD" uv run --no-project --with pyyaml python -m aimanager.scripts.smoke_budget_block --master-key aimanager-local-master-key --business-base-url http://localhost:4000 --admin-base-url http://localhost:4001 --project-directory "$PWD" --budget 0.005 --poll-attempts 60 --poll-interval 1
LITELLM_MASTER_KEY=aimanager-local-master-key PYTHONPATH="$PWD" uv run --no-project --with pyyaml python -m aimanager.scripts.smoke_key_lifecycle --master-key aimanager-local-master-key --business-base-url http://localhost:4000 --admin-base-url http://localhost:4001 --project-directory "$PWD" --poll-attempts 60 --poll-interval 1
LITELLM_MASTER_KEY=aimanager-local-master-key PYTHONPATH="$PWD" uv run --no-project --with playwright python -m aimanager.scripts.smoke_admin_ui_key_creation --master-key aimanager-local-master-key --admin-base-url http://127.0.0.1:4001 --browser-executable "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --timeout 60
LITELLM_MASTER_KEY=aimanager-local-master-key PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.smoke_postgres_down --master-key aimanager-local-master-key --business-key "$EMPLOYEE_VIRTUAL_KEY" --base-url http://localhost:4000 --timeout 10
PYTHONPATH="$PWD" uv run --no-project python -m aimanager.scripts.smoke_admin_boundary --business-base-url "$AIMANAGER_BUSINESS_BASE_URL" --public-admin-url "$AIMANAGER_PUBLIC_ADMIN_URL" --allowed-sso-redirect-host sso.company.example --require-business-base-url --require-public-admin-url
```

## 当前状态

- 代码来源：`https://github.com/gongqi210/litellm`
- 当前工作分支：`codex/bootstrap-aimanager`
- CodeGraph：已初始化，用于后续结构化检索
- 当前交付方式：配置 overlay + AiManager ASGI wrapper + LiteLLM CLI wrapper，不改 LiteLLM SDK
- 项目级 policy gate 已补齐：`PROJECT_TYPE=software-cli`、`cli/main.py --help/--json`、`docs/architecture/allowed_deps.yml`、`logs/README.md`、`aimanager/scripts/project_policy_check.py`、根目录 fallback shim `scripts/project_policy_check.py` 和 `make policy-check`；检查范围限定 AiManager-owned overlay，避免误伤上游 LiteLLM fork。
- 当前 compose 要求：默认 `aimanager` 服务使用 Docker `target: runtime`，入口为 `python -m aimanager.litellm_entrypoint --config=/app/config.yaml --host=0.0.0.0 --port=4000 --enforce_prisma_migration_check`，`AIMANAGER_ROUTE_SURFACE=business`，`AIMANAGER_DATABASE_READY_CHECK_ENABLED=True`，`AIMANAGER_WORK_CONTEXT_ENFORCEMENT_ENABLED=True`，`LITELLM_LOCAL_MODEL_COST_MAP=True`，并显式从 shell 或 `aimanager/.env` 透传 `LITELLM_MASTER_KEY`、`YCAPI_BASE_URL`、`YCAPI_API_TOKEN`；entrypoint 会把 `--config` 写入 `CONFIG_FILE_PATH`，供 ASGI runtime image-cost registration 使用。
- 当前管理面要求：`aimanager-admin` 只在 `admin` profile 渲染，使用同一镜像和入口，`AIMANAGER_ROUTE_SURFACE=management`，`AIMANAGER_RBAC_ENABLED=True`，`AIMANAGER_DATABASE_READY_CHECK_ENABLED=True`，`LITELLM_LOCAL_MODEL_COST_MAP=True`，端口只绑定 `127.0.0.1:4001:4000`；本地 Postgres 只绑定 `127.0.0.1:${AIMANAGER_POSTGRES_PORT:-15440}:5432`，避免占用宿主机默认 `5432`。
- M1-A 已完成本地自动化子项：非零 chat/image 价格校验、`ycapi-image-1` 使用 `input_cost_per_image`、禁 `output_cost_per_image`、禁 `pass_through_endpoints`、禁 NaN/inf/非数字价格、ASGI policy 默认拒绝、provider passthrough/Google native/`/config/update`/模型写路径封堵、`.env.example` 非密钥占位符检查。
- M1-B 已完成本地自动化、API 运行态、Admin UI key governance 和生产 key inventory 合同子项：key 创建请求必填 `user_id`、`team_id`、显式模型、预算、RPM/TPM、有效期、owner、部门、项目、成本中心、场景、审批人、内外部属性；management surface `POST /key/generate` 已在进入 LiteLLM 前调用 `normalize_key_request`，缺治理元数据返回 400 `aimanager_key_governance_invalid` 并记录 `policy_blocked` 审计事件；Admin UI 发送的数字字符串会规范化为数值；运行中 admin proxy smoke 已验证该 400 响应和日志无 Authorization/Bearer 泄漏；有效 shared key 创建已验证返回员工/团队/模型/预算/限流/有效期及部门/项目/成本中心/场景/审批人 metadata，且通过 `metadata.shared_key=true` 写入 LiteLLM 实际读取的 `metadata.enforced_params=["user","metadata.scenario_l1","metadata.end_user_principal"]`，AiManager OSS guard 会在推理期强制这些字段并记录 `enforced_params_blocked`，测试 key 已删除；真实 Chrome/Admin UI smoke 已验证 UI-originated 请求缺 metadata fail-closed，补齐治理字段后创建成功并删除 disposable key。`export_key_inventory` 可从 LiteLLM management surface 安全导出 metadata-only 全量 key inventory，丢弃 raw key/token/hash 并阻断分页片段或 secret-like metadata 输出；`validate_key_inventory` 用于生产存量/导入 key 清单门禁：缺 `AIMANAGER_KEY_INVENTORY_FILE`、缺 24 小时内且不超过 5 分钟未来偏差的全量导出 provenance、导出过期/明显来自未来、缺员工监控制度/名册/确认 evidence 或 active key 数为 0 时 BLOCKED，未替换模板标记/占位值、active legacy/wildcard/unbounded/missing duration/missing metadata key、声明总数不匹配、shared_key marker 非布尔、active key `user_id` 未出现在 active roster、未确认最新监控制度、部门不匹配或 raw secret-like inventory FAIL。
- M1-C 已完成本地财务导出、observability、live-token 口径和生产 readiness bundle 子项：usage/spend 记录可归一化真实 LiteLLM `SpendLogs` 形态字段（`startTime`、`call_type`、`openai/` provider 前缀、嵌套 metadata），可按日期/月度聚合到部门/项目/成本中心/员工/key/model/endpoint/currency/pricing_version；`aimanager.scripts.export_finance` 可从 SpendLogs JSON/CSV 和 ycapi 月账单 JSON/CSV 生成 `aimanager_usage_daily.csv`、`aimanager_finance_monthly.csv`、`aimanager_reconciliation.csv`，并完成 AiManager 与 ycapi 月度金额差异对账，`needs_review` 计数会返回给 readiness；`passthrough_blocked`/`policy_blocked`/`enforced_params_blocked`/`budget_blocked`/`key_frozen`/`key_revoked` 审计事件合同已存在；`aimanager.runtime_metrics` 在 management surface 提供 `/metrics`，`aimanager.scripts.export_observability` 可生成 failure rate、429/5xx、预算阻断、passthrough 拦截、enforced params 拦截、缺 request id 等机器可读 alert；`aimanager.scripts.route_observability_alerts` 可将 alert report 渲染/投递为企业微信 markdown，支持 dry-run、severity 过滤、缺 webhook BLOCKED 和 WeCom `errcode=0` 成功判定；`aimanager.scripts.smoke_live_ycapi` 可在缺真实 `YCAPI_API_TOKEN` 时明确返回 BLOCKED，有 token 时默认只读检查 ycapi `/models` 期望模型，`--run-inference-roundtrip` 会在模型均已列出后执行受控 chat/image roundtrip，只保留状态码、usage/image 计数和 request id，不输出 token、URL、prompt 或响应正文；`aimanager.scripts.production_readiness_bundle` 将 AC-15/AC-20/AC-19/AC-08-KEY-INVENTORY/AC-16-WeCom/AC-12-13-Finance/AC-POLICY 聚合为一个脱敏 JSON 证据包，AC-12/13 对账出现 `needs_review` 直接 FAIL，AC-19 强制启用 live chat/image roundtrip 并输出脱敏元数据，AC-08 会强制 key inventory 与员工名册/确认记录交叉验证并输出 employee acknowledgment 摘要，AC-15 保留逐探测项 method/path/status/status_code/policy_code/detail，AC-20 先保留缺 metadata fail-closed 的 method/path/model/status/status_code/policy_code，再保留合法 work-context roundtrip 的 check_type/request_id/usage_present/image_result_count/work_context_present 等脱敏证据，且不输出员工 key、prompt、assistant text、图片内容或响应正文；缺生产输入时返回 exit code 2。
- M1-A 已完成运行态 smoke 子项：真实容器启动、LiteLLM Prisma migration 和 lifespan 完整执行、health 200、员工 virtual key 下 `/v1/models` 仅返回 `gemini-2.5-flash`/`deepseek-chat`/`ycapi-image-1`、35 条 provider/native/config/cache/reload/model-write/uncommitted/business-token 路由封堵矩阵全部返回 AiManager 403 policy code。
- M1-A 已完成 policy audit 子项：被封请求在返回 403 前写出结构化 `aimanager_audit_event`；provider/native 使用 `passthrough_blocked`，config/cache/reload/model/default-denied 使用 `policy_blocked`；审计日志不记录 Authorization/Bearer token。
- M1-A 已完成业务面/管理面拆分子项：business surface 阻断 `/ui`、`/key/*`、`/team/*`、`/user/*`、`/spend/*`、`/global/spend` 等 LiteLLM 管理路由；management surface 放行 UI/key/team/user/budget/spend 管理路由，但仍封堵 provider passthrough、Google native、runtime config、cache/reload、pass-through endpoint 和 model write。运行态已验证 business `4000` 上 `/ui` 为 AiManager 403，admin `127.0.0.1:4001` 上 `/ui` 为 LiteLLM 307，admin `/config/field/update` 仍为 AiManager 403。
- M1-B 已完成 AC-14 本地 RBAC 子项：management surface 在 `AIMANAGER_RBAC_ENABLED=True` 时对 `/key`、`/team`、`/user`、`/customer`、`/organization`、`/budget`、`/spend`、`/global/spend` 写方法执行 trusted-header 预检；`finance`/`ceo`/`audit`/`proxy_admin_viewer`、未知角色和缺角色返回 403 `aimanager_rbac_denied` 并写 `policy_blocked` 审计，`proxy_admin` 可继续进入 LiteLLM 管理流，只读角色可访问 spend/activity 报表；business surface 不会因 `x-aimanager-role` 解锁管理路由。生产仍必须依赖 LiteLLM 原生认证授权和受控 SSO/反代剥离伪造 role header。
- M1-C 已完成 AC-17 Postgres down 运行态验收：`AIMANAGER_DATABASE_READY_CHECK_ENABLED=True` 时 AiManager 对 readiness、业务允许路由和管理允许路由执行 Postgres 协议握手式 DB-ready 预检；隔离 compose project `aimanager_postgres_down` 停止 `db` 后，`aimanager.scripts.smoke_postgres_down` 验证 readiness 503、provider passthrough 仍为 AiManager 403、员工 virtual key business chat 返回 sanitized 503 `aimanager_database_unavailable`，不进入 downstream LiteLLM/ycapi。
- M1-A 已完成启动期离线化、image 计价注册、shared-key enforced params guard 和错误契约子项：`aimanager.litellm_entrypoint` 在导入 LiteLLM 前强制 `LITELLM_LOCAL_MODEL_COST_MAP=True`，避免 LiteLLM import-time 远程 cost map TLS 拉取阻塞网关启动；`aimanager.asgi` 在 LiteLLM proxy 加载后注册 `openai/ycapi-image-1` 的 `input_cost_per_image` 和 `AiManagerEnforcedParamsGuard`，避免图片 spend 记 0，并在 OSS 路径强制 shared-key 工作上下文；允许路由的下游 429 JSON error 会保留上游 `error` 并注入顶层 `request_id`，401/403/404 状态类型回退稳定，成功流式响应原样透传，下游 SSE error event 和非 JSON 5xx 都会转为不泄密的 OpenAI-compatible JSON。
- M1-A 已完成运行态 spend 验收：mock ycapi + rebuilt business/admin proxy + `aimanager.scripts.smoke_spend_logs` 证明 `LiteLLM_SpendLogs` 写入非零 spend，chat `openai/gemini-2.5-flash=3.3e-06`，image `openai/ycapi-image-1=0.01`。
- M1-A 已完成 streaming usage/spend 防绕过本地合同：`aimanager/config.yaml` 要求 `general_settings.always_include_stream_usage=true`，`validate_config` 缺失/关闭时 FAIL；`aimanager.asgi` 对 `POST /v1/chat/completions` 且 `stream=true` 的请求体强制写入 `stream_options.include_usage=true`，即使调用方显式传 `false` 也会改写，同时限制 chat body 缓冲上限并对异常 body fail-closed；`aimanager.scripts.mock_ycapi` 支持 OpenAI-compatible SSE，并在 include_usage 为 true 时输出最终 `usage` chunk。该项封堵本地流式计费 body-level 绕过，不替代生产 live ycapi 与财务对账证据。
- M1-C 已完成 AC-10 运行态预算阻断验收：mock ycapi + rebuilt business/admin proxy + `aimanager.scripts.smoke_budget_block` 证明一次性治理 key `max_budget=0.005` 先经 image 成功消费 `0.01`，随后同 key 的 business chat 请求返回 HTTP 429 `budget_exceeded`；响应和 LiteLLM 容器日志均记录 `Budget has been exceeded! ... Current cost: 0.01, Max budget: 0.005`，AiManager 同时输出结构化 `aimanager_audit_event`，`event_type=budget_blocked`、`severity=high`、`reason=budget_exceeded`。`LiteLLM_VerificationToken.spend` 可能因批量写延迟仍为 `0.0`，阻断主证据是实时 spend counter 触发的 429。
- M1-C 已完成 AC-11 运行态 key 生命周期验收：management surface `POST /key/block`/`POST /key/delete` 现在要求 `x-aimanager-actor` 与 `x-aimanager-reason`/`x-aimanager-disposition-reason`，缺失时返回 HTTP 400 `aimanager_key_lifecycle_invalid` 且不进入 LiteLLM；所有允许 `/v1/*` 推理路由在进入 LiteLLM 前按 LiteLLM 凭证头优先级提取 virtual key，并查询 `LiteLLM_VerificationToken.blocked` 和 `LiteLLM_DeletedVerificationToken`，避免 business/admin 两个独立进程的本地 key cache 不一致；business surface 额外拒绝 `LITELLM_MASTER_KEY`/`YCAPI_API_TOKEN`；mock ycapi + rebuilt business/admin proxy + `aimanager.scripts.smoke_key_lifecycle` 证明冻结/撤销后的 key 通过 Authorization、API-Key、x-api-key、x-goog-api-key、Ocp-Apim-Subscription-Key、x-litellm-api-key 均被业务面拒绝，admin 容器日志包含 `key_frozen` 和 `key_revoked` 结构化审计事件，带处置人、原因、request id、key alias 和治理维度。
- M1-C 已完成 AC-07 本地错误契约验收：`pytest aimanager/tests/test_policy.py aimanager/tests/test_streaming_error_contract.py` 证明 AiManager policy error 与下游 LiteLLM/ycapi 原生错误均返回 OpenAI-compatible `error` + 顶层 `request_id`；下游 `x-litellm-call-id` 会同步到 body `request_id` 和响应头；下游 JSON/text 错误和 HTTP 200 SSE error event 不会把 Bearer、`sk-*`、ycapi token 文本或 DSN 密码回显给客户端，跨 chunk 拆开的流式 secret 也会被脱敏。AC-17 已覆盖 mock 下游 429/5xx、provider passthrough 不回退和 Postgres down fail-closed。
- M1-C 已完成 AC-06 本地合同、示例、business-surface work-context 强制和 disposable-key 运行态验收：`aimanager.scripts.smoke_sdk_compat` 只读取员工 LiteLLM virtual key，不读取/打印 `YCAPI_API_TOKEN`，并在误用 `YCAPI_API_TOKEN` 变量或同值密钥时先返回 FAIL；覆盖业务面 `/v1/models`、`gemini-2.5-flash`/`deepseek-chat` chat、`ycapi-image-1` image `response_format=b64_json`、base64 `data:` URL vision、完整工作 metadata 和 OpenAI-compatible 响应形态；`AIMANAGER_WORK_CONTEXT_ENFORCEMENT_ENABLED=True` 时 business surface 对 chat/image 生成请求在进入 LiteLLM 前校验 `user` 与 work-context metadata，缺失、非法场景或 `user` 与 `metadata.end_user_principal` 不一致返回 400 `aimanager_work_context_invalid` 且不回显 prompt；`aimanager.scripts.smoke_work_context_enforcement` 已可验证 chat/image 缺 `metadata` 均返回 400 `aimanager_work_context_invalid`，并可在 fail-closed 全部通过后用合法 work-context metadata 跑 chat/image 正向 roundtrip，要求 200、request id、chat usage 和 image 结果数；该 smoke 不替代 AC-08 key inventory 或员工身份有效性证据；`aimanager.scripts.smoke_runtime_sdk_compat` 已在本地 mock ycapi + rebuilt business/admin surface 上通过，输出 `PASS`、`cleanup=PASS`、`endpoints=models,chat:gemini-2.5-flash,chat:deepseek-chat,image,vision:gemini-2.5-flash`；缺 master key 或员工 key 返回 BLOCKED，清理 HTTP/transport 失败返回 FAIL。该项证明本地运行态 SDK/接口兼容，不代表生产 live ycapi chat/image 已验证；生产 live ycapi token 仍由 AC-19 单独验收。
- M1-B Admin UI key 创建自动化已完成：`aimanager.scripts.smoke_admin_ui_key_creation` 在管理面真实 UI 中验证缺治理 metadata 的 `/key/generate` 请求返回 400，补齐治理字段后返回 200，且 disposable key 自动删除。
- M1-B/AC-15 生产管理面边界 preflight 已补齐脚本化验证入口：`aimanager.scripts.smoke_admin_boundary` 可检查业务 URL 在伪造 `x-aimanager-role=proxy_admin` 时仍阻断 `/ui`、`/key/generate`、`/v2/key/info`、`/metrics`、`/config/field/update` 等管理/配置路由，且不发送 `Authorization`/master key，避免失败模式下误创建生产资源；同时检查公开管理 URL 是否不可达、被 401/403/404 阻断或只跳转到 allowlisted SSO；`production_readiness_bundle` 会保留逐探测项证据；缺真实生产 URL 时保持 `BLOCKED`。
- M1-C 仍需补强：生产试点前需在真实生产 management surface 上运行 `export_key_inventory`，生成 24 小时内 metadata-only LiteLLM virtual key 全量 inventory，并通过 `AIMANAGER_KEY_INVENTORY_FILE`、`AIMANAGER_EMPLOYEE_MONITORING_POLICY_FILE`、`AIMANAGER_EMPLOYEE_ROSTER_FILE` 和 `AIMANAGER_EMPLOYEE_ACKNOWLEDGMENT_FILE` 证明所有 active key 都已治理、总数声明匹配导出记录，且 key `user_id` 属于已确认最新监控制度的 active employee；生产 business surface 需提供 `AIMANAGER_BUSINESS_BASE_URL` 与 `AIMANAGER_EMPLOYEE_VIRTUAL_KEY`，跑 `make work-context-enforcement-readiness` 证明 chat/image 缺 `metadata` 会 fail-closed 为 400 `aimanager_work_context_invalid`，且合法 work-context metadata 能通过同一个员工 virtual key 获得 200、request id、chat usage 和 image 结果数；生产月结前需补真实 ycapi 月账单文件或 API 输出作为外部证据，且 `production_readiness_bundle` 要求 AiManager spend 与 ycapi bill 两侧都有非零计费金额且 reconciliation 无 `needs_review`，空文件或占位行保持 BLOCKED、明显错账保持 FAIL；生产告警路由已有 WeCom 脚本化入口，但仍需在部署侧用真实 `AIMANAGER_WECOM_WEBHOOK_URL` 和 alert report 跑出 live routing 证据；live ycapi roundtrip 已有脚本化入口，但仍需生产侧提供真实 `YCAPI_API_TOKEN` 跑出 `/models` + chat/image PASS 证据；M1 试点前以 `production_readiness_bundle` JSON 全 PASS 作为统一收口。
- M1-C 已完成 AC-POLICY 生产政策 attestation gate：`production_readiness_bundle` 现在要求 `AIMANAGER_PRODUCTION_POLICY_ATTESTATION_FILE` 指向结构化 JSON，校验 showback/chargeback、价格审批、ycapi token 月预算/RPM 有限正数、员工只拿 LiteLLM virtual key 且不可见 ycapi token、数据边界、finance/security/legal 三方规范化后独立审批；缺文件 BLOCKED，坏 JSON/字段缺失/矛盾/非有限限额/三方审批人不独立 FAIL，secret-like 值脱敏后 FAIL；`docs/aimanager/production_policy_attestation.example.json` 的示例结构有单测防漂移。
- M2-A 已完成 AC-20/21/22 本地合同：`aimanager.work_context` 和 `aimanager.scripts.validate_work_context` 可校验非 SDK 入口提交的 work-context JSON；preflight 模式要求最终员工主体、部门、一级/二级场景、内外部、渠道、项目或客户、敏感级别均为非空字符串，审批要求为布尔值；外发市场内容还要求非空 brief、审核人、审批政策引用和品牌安全清单；closure 模式额外要求草稿、人工审核、定稿、外发审批、归档和复盘引用；CLI 明确区分 PASS=0、FAIL=1、BLOCKED=2。AC-23 已有本地轻量网页入口合同，仍因生产 live ycapi、SSO/受控身份注入和真人 5 分钟试用未完成而保持 BLOCKED。
- M2-A 已完成 AC-23 后端/CLI 适配合同和本地轻量 Web 入口合同：`aimanager.lightweight_entry` 和 `aimanager.scripts.submit_lightweight_entry` 可将扁平 WeCom/轻量页/部门管理员表单 dry-run 为治理后的 OpenAI-compatible `/v1/chat/completions` 请求体，并可通过员工 LiteLLM virtual key 原始 HTTP 提交到 business surface；对外发市场内容，轻量入口会把已预检的 `workflow` 和 `brand_safety` 一并写入 request metadata，避免 business-surface work-context enforcement 拦截合法非 SDK 试用；`aimanager.lightweight_web` 提供可点击内部协作/管理试用页，作为 business gateway 客户端运行，不修改 AiManager business allowlist；页面从服务端环境默认值注入员工、部门、项目、成本中心、币种和价格版本，提交时使用服务端员工 LiteLLM virtual key，HTML 不包含 key、Authorization 或 ycapi token，secret-like prompt 不回显，模型回答做 HTML escape。该项已有本地非 SDK UI 合同证据，但仍不代表生产 WeCom/SSO 身份注入、live ycapi 或真实市场/业务人员 5 分钟合规试用完成，AC-23 继续保持 BLOCKED。
- M2-A 已完成 AC-23 轻量试用证据采集入口：`aimanager.scripts.capture_lightweight_trial_evidence` 会把一次成功的 `submit_lightweight_entry` 结果转换为 `business_trial_acceptance_bundle` 可消费的 evidence 文件，只保留 allow-listed metadata、endpoint、model、status、外部传入的真实 request id、非零 spend、时间和观察人；work context 支持 `project_id` 或 `customer_id` 至少一个；采集前要求提交结果保留 preflight `entry.work_context.status=PASS` 与匹配的 normalized work-context 标识，阻断裁剪过或仅翻转状态字段的提交结果自行声明 `work_context_status=PASS`，并降低完整手写 JSON 风险；prompt、assistant text、headers、Authorization、cookie、employee key、ycapi token 和 WeCom webhook 不会落盘；非 PASS 提交、缺失或失败 work-context proof、非 preflight proof、normalized-context 标识不匹配、secret-bearing field、secret-like 输出值、secret-like assistant echo 和 raw-key-looking alias 会返回 BLOCKED/FAIL 且不写 evidence 文件。该项降低 AC-23 真人试用证据采集的泄密和手写 JSON 风险，但仍不能替代生产 live ycapi、SSO/受控身份注入和真人 5 分钟合规试用。
- M2-A/M2-B 已完成业务试用准入统一 gate：`aimanager.scripts.business_trial_acceptance_bundle` 会把 M1 `production_readiness_bundle` 的 AC-15/20/19/08-key-inventory/16/12-13/AC-POLICY 和 M2 AC-23/26 合并为一个 PASS/FAIL/BLOCKED JSON；AC-23 evidence 使用 typed schema 校验非 SDK 业务人员、受控身份注入、员工 virtual key、未暴露 ycapi token、request id、允许模型/endpoint、非零 spend、工作场景 PASS、HTML escape/no-secret-echo、见证人、0-300 秒试用时长、captured_at 不早于完成时间且不晚于本轮 bundle generated_at，并拒绝 prompt/response/content、Authorization/cookie/token 字段和 `Bearer`/`sk-*` 值；即使试用 JSON 完整，AC-23 也只有同轮 AC-19 live ycapi 为 PASS 时才可 PASS。空环境当前返回 exit code 2，9 项业务口径 BLOCKED。
- M2-A/M2-B 已完成启动缺口计划生成器：`aimanager.scripts.generate_launch_gap_plan` 只读消费 M1/M2 gate JSON，去重重复 AC，按 `FAIL` 优先再 `BLOCKED` 排序，给出 owner、required env/files、重跑命令和下一步动作；AC-08 缺口命令收敛为 `make key-inventory-readiness`，同时要求 `AIMANAGER_KEY_INVENTORY_FILE`、`LITELLM_MASTER_KEY`、员工监控制度、员工名册和确认记录，用于先导出 LiteLLM metadata-only key inventory、再执行治理 + 员工确认交叉校验并刷新统一 readiness；AC-15 缺口命令收敛为 `make admin-boundary-readiness`，用于生产业务 URL/公开管理 URL/SSO 边界 preflight 后刷新统一 readiness；AC-20 缺口命令收敛为 `make work-context-enforcement-readiness`，用于 smoke 生产 business surface 缺 `metadata` fail-closed，再验证合法 work-context chat/image roundtrip 并刷新统一 readiness；AC-16 企业微信缺口命令收敛为 `make wecom-alert-readiness`，先 dry-run 渲染 payload，再由 production readiness 做唯一 live 投递；AC-12/13 财务缺口命令收敛为 `make finance-readiness`，先生成 usage/monthly/reconciliation CSV 再刷新 readiness；AC-19/AC-POLICY/AC-23/AC-26 继续分别收敛为 `make live-ycapi-roundtrip`、`make production-policy-readiness`、`make lightweight-trial-evidence-capture`、`make employee-monitoring-validate`；无输入时返回 exit code 2 / BLOCKED，输出 JSON/Markdown 会脱敏 WeCom webhook、Bearer 和 `sk-*`。该项只是把外部证据缺口变成可分派计划，不替代生产 URL、work-context 生产 smoke、live ycapi、WeCom、财务账单、政策审批、真人试用或 HR/法务确认。
- M2-A/M2-B 已完成证据移交包生成器：`aimanager.scripts.generate_evidence_handoff` 只读消费 `launch-gap-plan.json`，按 owner 输出 `evidence-handoff.json/md` 和各 owner Markdown 请求文件，保留原 `FAIL`/`BLOCKED` 状态；畸形 JSON 结构、非对象 gap、或顶层非 PASS 但缺少 actionable gap 时会生成输入级 `FAIL`/`BLOCKED` gap，避免空 gap 退化为 PASS。输出会重复 required env/files、重跑命令和下一步动作，并提示接收人不得粘贴密钥、原始 prompt/response、cookie、员工 key、ycapi token 或客户内容；该项用于把外部证据收集分派给财务、运维、市场、HR/法务/安全等角色，不替代任何真实生产 evidence。
- M2-A/M2-B 已完成外部证据模板包生成器：`aimanager.scripts.generate_evidence_template_pack` 只读消费 `launch-gap-plan.json` 和 evidence handoff 结构，生成 key inventory、finance spend/ycapi bill CSV、observability report、AC-23 trial evidence、AC-26 roster/acknowledgment、AC-POLICY attestation 与 `evidence-env.template`；模板显式标记 `TEMPLATE_DO_NOT_SUBMIT`，`YCAPI_API_TOKEN` 只保留 secret-manager 注释、不写 export/value，key inventory 模板不含 raw key，并包含 full-export provenance、`expected_total_key_count` 和 `duration` 占位，Webhook 留空，继承文本脱敏，且单测证明这些模板不能让 `production_readiness_bundle` 伪装 PASS。该项只降低外部 owner 收集证据的格式成本，不替代真实生产 evidence。
- M2-A/M2-B 已完成外部证据 intake 校验器：`aimanager.scripts.validate_evidence_intake` 只读扫描 owner 回填目录或 gate env 指向的 evidence 文件，未配置任何 env-pointed evidence 文件、未替换 `TEMPLATE_DO_NOT_SUBMIT`/`replace-with-*` 模板、header-only CSV 或空 evidence 目录返回 BLOCKED，raw prompt/response/header/cookie/Authorization 和 secret-like 值返回 FAIL，但允许 `raw_prompt_access=prohibited` 这类表达禁止原文访问的 governance policy 字段；AC-23 trial evidence 会复用 typed schema 校验，AC-POLICY attestation 会复用生产政策规则，安全但矛盾的 evidence 不再只因无 secret 而 PASS；`run_acceptance_gate` 会把 `AIMANAGER_KEY_INVENTORY_FILE` 与其他 env-pointed evidence 一起送入 intake。该项用于在回填 evidence 进入 production/business gate 前先做泄密、空输入、模板占位和关键 schema 门禁，不替代真实生产 evidence。
- M2-B/M3 已完成最终验收覆盖矩阵 gate：`aimanager.scripts.acceptance_coverage_matrix` 只读解析验收标准文档，确认 AC-01..AC-26 与 AC-POLICY 均有登记本地 artifact 或 M1/M2 bundle check，检查脚本/测试文件存在，叠加 `production_readiness_bundle` 与 `business_trial_acceptance_bundle` 状态，并在已提供 bundle 时检测 `AC-08-KEY-INVENTORY`、`AC-12-13-FINANCE`、`AC-16-WECOM`、AC-20、AC-23、AC-26、AC-POLICY 等 check 是否漂移；无 bundle 时结构性通过但生产证据项保持 `BLOCKED`，输出 JSON/Markdown 会脱敏 WeCom webhook、Bearer 和 `sk-*`。该项是验收口径防漂移 gate，不替代真实生产证据或启动缺口计划。
- M2-B/M3 已完成最终验收报告生成器：`aimanager.scripts.generate_final_acceptance_report` 只读消费业务试用准入 bundle、启动缺口计划、验收覆盖矩阵和可选 evidence intake，生成最终 go/no-go JSON/Markdown；缺输入或未解除外部证据返回 BLOCKED/条件结论，坏 JSON、坏 criteria 或失败 intake 项返回 FAIL，只有主输入均 PASS、无启动 gap、覆盖矩阵包含非空 criteria 对象列表且已提供 evidence intake 为 PASS 时才 PASS；blocker 输出保留 owner、required env/files、重跑命令和下一步动作，同时给出来源级 blocker 数和按 id 去重 blocker 数，并统一脱敏 Bearer、`sk-*`、DSN 密码和 WeCom webhook。该项把最终验收从手写报告提升为机器门禁，但不替代生产 live ycapi、WeCom、财务账单、政策审批、真人试用或 HR/法务确认。
- M2-B/M3 已完成一键验收 gate：`aimanager.scripts.run_acceptance_gate` 和 `make acceptance-gate` 会在一个 run-scoped 目录中生成 `production-readiness.json`、`business-trial-acceptance.json`、`launch-gap-plan.json/md`、`evidence-handoff/`、`evidence-template-pack/`、`evidence-intake.json/md`、`acceptance-coverage.json/md`、`final-acceptance-report.json/md` 和 `acceptance-gate.json/md`；同轮 production readiness 只执行一次并复用于 business trial，env 指向的 owner evidence 文件会先做 intake 校验并传入 final report 再折叠进 gate 状态，避免手工串联时混入旧 `/tmp` 文件；2026-07-01 空环境实跑 `/tmp/aimanager-next-gate` 返回 exit code 2 / BLOCKED，`final=BLOCKED`、`blockers=29`、`unique=14`，主要缺口仍是生产 key inventory、财务对账、生产 URL 边界、work-context 生产 fail-closed/valid-context smoke、WeCom、live ycapi、真人轻量试用、员工监控制度和生产政策审批；evidence intake 会因未配置任何 `AIMANAGER_*_FILE` evidence env 单独 BLOCKED，unsafe env evidence 会 FAIL，安全 env evidence 可 PASS，全绿注入路径必须带 PASS evidence 才可 PASS，所有 gate 写出的 artifact 会脱敏 secret-like 值。该项是最终验收编排入口，不替代生产 key inventory、work-context 生产 smoke、live ycapi、WeCom、财务账单、政策审批、真人试用或 HR/法务确认。
- M2-B/M3 已补齐一键验收 gate golden E2E：提交 `aimanager/tests/fixtures/acceptance_golden/` 合成 metadata-only 证据，使用真实 `collect_production_readiness`、`collect_business_trial_acceptance`、launch gap、evidence intake、coverage 和 final report collector 跑出 PASS，只对 admin boundary/live ycapi/WeCom 三类外部网络 runner 做测试注入；同时覆盖 key owner 不在员工名册、ycapi bill 金额大幅偏离 spend、生产政策缺 legal 审批、AC-23 captured_at 晚于 gate 出包时间、员工确认早于政策发布五类篡改都必须让 gate FAIL；该 fixture 只证明本地 gate 编排、collector 接线和安全 evidence intake 的全绿/拒绝路径，不替代真实生产 key inventory、live ycapi、WeCom、财务账单、政策审批、真人试用或 HR/法务确认。
- M2-B 已完成 AC-24 本地经营总览报表合同：`aimanager.business_overview` 和 `aimanager.scripts.generate_business_overview` 可将 `aimanager_finance_monthly.csv`、预算 CSV/JSON 与 observability JSON 聚合为总经理可读 JSON/Markdown，展示月费用、预算消耗、异常事件、TOP 部门、TOP 项目和 TOP key；缺预算/财务/观测输入返回 BLOCKED，多币种混杂返回 FAIL。该项是 CLI/Markdown 报表证据，不代表生产自助门户或实时 dashboard。
- M2-B 已完成 AC-25 本地月结包合同：`aimanager.monthly_close` 和 `aimanager.scripts.generate_monthly_close_package` 可将 `aimanager_finance_monthly.csv`、`aimanager_reconciliation.csv` 与财务调整台账生成 JSON、调整后 CSV 和 Markdown，覆盖补记、冲销、归属调整和差异处理；残留非零 `unassigned` 或未解决 `needs_review` 差异返回 BLOCKED，非法冲销、重复调整编号、未知对账差异或不支持的处理状态返回 FAIL；当月度导出没有 `entry_id` 时，使用 `month|department_id|project_id|cost_center_id|user_id|key_alias|model|endpoint|currency|pricing_version` 复合行键定位真实导出行；输出不序列化 token-like 额外字段。该项是本地 CLI/月结包证据，不代表生产 ERP 过账、总账写回或真实财务审批流上线。
- M2-B 已完成 AC-26 本地制度/证据校验合同和机器可读 policy register：`aimanager.employee_monitoring` 和 `aimanager.scripts.validate_employee_monitoring_policy` 可校验员工监控制度、员工名册和确认导出，要求 metadata-only 监控字段、禁止 prompt/response/raw IP/customer content 检查、7-180 天保留期、非工作时间异常规则、疑似 key 共享规则、受限审阅角色、禁止直属经理自助审阅、HR/法务惩戒边界、员工申诉渠道和最新版本确认覆盖；`docs/aimanager/aimanager-employee-monitoring-policy.json` 已提交并有单测证明符合 validator 契约，`.env.example` 默认指向该文件；缺真实 roster/ack 文件仍 BLOCKED，制度矛盾或确认缺口 FAIL，输出不序列化 token-like 额外字段。该项是本地 policy/evidence contract，不代表真实 HR/法务发布或全员确认已完成；AC-26 在验收矩阵继续保持 BLOCKED，待真实公司 evidence 跑出 PASS。
